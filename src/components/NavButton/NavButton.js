import classes from "./NavButton.module.css";

const NavButton = () => {
    return ( 
        <button className={classes.NavButton}>
Make a request
        </button>
     );
}
 
export default NavButton;